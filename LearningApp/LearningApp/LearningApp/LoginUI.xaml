<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="LearningApp.LoginUI">
    <ContentPage.Content>
        
        <StackLayout VerticalOptions="Center" HorizontalOptions="FillAndExpand" Padding="40" Spacing="30">
            
            <StackLayout>
                <Frame>
                    <Image Source="python.png" WidthRequest="120" HeightRequest="120"/>
                </Frame>
            </StackLayout>
            <StackLayout Spacing="20">
                <Entry x:Name="txtUsername" Placeholder="Username" TextColor="Black" PlaceholderColor="Gray"></Entry>
                <Entry x:Name="txtpassword" Placeholder="Password" IsPassword="True" TextColor="Black" PlaceholderColor="Gray"></Entry>
                <Button VerticalOptions="Center"
                    BorderWidth="1.5"
                    CornerRadius="50"
                    TextColor="Black"
                    BackgroundColor="Transparent"
                    BorderColor="Black"
                    Clicked="LoginClicked"
                    Text="Login"/>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>